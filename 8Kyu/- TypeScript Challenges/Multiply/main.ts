// Multiply

// The code was not written correctly. Fix it.

// My Solution
export const multiply = (a: number, b: number): number => a * b;

// Other Solutions
export function multiply(a: number, b: number): number {
  return a * b;
}
